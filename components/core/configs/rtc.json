{
    "timezone": "UTC",
    "publish_to_shared_state": true,
    "publish_interval_s": 60,
    "time_format": "%Y-%m-%d %H:%M:%S",
    "ntp": {
        "enabled": false,
        "server": "pool.ntp.org",
        "sync_interval_s": 3600
    }
}
